# 114121031 - GANISHK D


#### Testscript
./add_to_path
. $HOME/.bashrc // Or relogin into the system
sudo genUser
sudo permit
sudo updateBranch //Option to run as root
sudo allotInterest
sudo makeTransaction
